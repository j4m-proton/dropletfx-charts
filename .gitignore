# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Node / Electron
node_modules/
desktop/node_modules/

# Electron build output (installers, packed app)
desktop/release/
desktop/release-fresh/
desktop/installer-build*/
desktop/dist/

# Frozen Python server (PyInstaller) + its build dirs
dfx-server.exe
server-dist/
server-build/

# Runtime config that may hold secrets (Google client secret, tokens)
config.json
*.env
.env
.env.*

# Editor / OS
.DS_Store
Thumbs.db

# Local Claude Code settings
.claude/settings.local.json
